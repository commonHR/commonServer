/*       MODULE DEPENDENCIES         */

/*         CHAT FUNCTIONS            */

exports.retrieveMessages = function(screenName) {

  // Retrieve messages from database (separate db function?)
  // Send messages to client

};


exports.sendMessage = function (conversation, screenName, recipient) { 

  //Send message to recipient
  //find conversation in the database, if it exists.  If not, add a conversation node.
  //Insert the latest message into the conversation "chain" 

};